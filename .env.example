# SMTP credentials used by the quote request API (src/app/api/rental-quote/route.ts)
SMTP_HOST=mail.cosma.co.th
SMTP_PORT=587
SMTP_USER=sales@cosma.co.th
SMTP_PASSWORD=
# Optional: defaults to SMTP_USER
SMTP_FROM=sales@cosma.co.th
# Optional: defaults to SITE.email in src/lib/constants.ts
MAIL_TO=sales@cosma.co.th
